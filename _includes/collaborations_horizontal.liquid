<div class="card-item col">
  <a href="{% if collaboration.redirect %}{{ collaboration.redirect }}{% else %}{{ collaboration.url | relative_url }}{% endif %}">
    <div class="card hoverable">
      <div class="row g-0">
        {% if collaboration.img %}
          <div class="card-img col-md-6">
            {% include figure.liquid loading="eager" path=collaboration.img sizes="(min-width: 768px) 156px, 50vw" alt="collaboration thumbnail" %}
          </div>
        {% endif %}
        <div class="{% if collaboration.img %}col-md-6{% else %}col-md-12{% endif %}">
          <div class="card-body">
            <h3 class="card-title text-lowercase">{{ collaboration.title }}</h3>
            <p class="card-text">{{ collaboration.description }}</p>
            <div class="row ml-1 mr-1 p-0">
              {% if collaboration.github %}
                <div class="github-icon">
                  <div class="icon" data-toggle="tooltip" title="Code Repository">
                    <a href="{{ collaboration.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
                  </div>
                  {% if collaboration.github_stars %}
                    <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                      <i class="fa-solid fa-star"></i>
                      <span id="{{ collaboration.github_stars }}-stars"></span>
                    </span>
                  {% endif %}
                </div>
              {% endif %}
            </div>
          </div>
        </div>
      </div>
    </div>
  </a>
</div>
